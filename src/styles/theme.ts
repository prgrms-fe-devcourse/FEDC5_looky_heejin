export const lightTheme: object = {
  theme_mode: "light",
};

export const darkTheme: object = {
  theme_mode: "dark",
};

const Theme = {
  lightTheme,
  darkTheme,
};

export default Theme;
